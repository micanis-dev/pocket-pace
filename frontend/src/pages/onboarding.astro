---
import Base from '../layouts/Base.astro';
import Onboarding from '../components/Onboarding';
import { getSession } from '../lib/server/auth';
if (!await getSession(Astro.cookies)) return Astro.redirect('/signup');
---
<Base title="初期セットアップ | Pocket Pace"><Onboarding client:load /></Base>
